# jarvis-ai-gateway
This is designed around the AWS cloud: private ECS Fargate, no public IP, internal load balancer, ECS task-role auth, and Bedrock Runtime through PrivateLink / endpoint-specific DNS override when enterprise DNS bypasses normal private endpoint behavior.
